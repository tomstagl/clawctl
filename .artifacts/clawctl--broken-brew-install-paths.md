TITLE: docs: remove broken Homebrew install references

BODY:
## Summary

Closes #9. The repo documented a Homebrew install path that doesn't exist —
there is no `tomstagl/homebrew-clawctl` tap, and `install/clawctl.rb` was
already deleted from `main`. `tasks/prd-clawctl-distribution-and-integration.md`
records the decision explicitly: *"No Homebrew tap. Decided: not a v1.0
channel."* The docs never caught up.

- `docs/mcp.md` — the prerequisites list named `brew install` as one of three
  ways to get `clawctl` on `PATH`. Now names only the `install.sh` curl
  one-liner (matching `README.md`) and `go build ./cmd/clawctl`.
- `CONTRIBUTING.md`'s "Releasing" section described two things that no longer
  apply: bumping a version in "the script header comment" (Bash-era; the Go
  binary is version-stamped at build time via `-ldflags
  "-X main.version=${GITHUB_REF_NAME}"` in `release.yml`, see
  `cmd/clawctl/main.go:19`) and "update the Homebrew formula's `url` and
  `sha256`" (the formula doesn't exist). Rewrote the section to describe the
  actual process: tag → `release.yml` builds and publishes → bump
  `.claude-plugin/plugin.json` → `CHANGELOG.md` entry.

`grep -rni "homebrew\|brew install\|brew tap" --include="*.md" .` now returns
hits only inside `tasks/prd-*.md` — historical PRD records, left untouched
per the issue's "Do not" list.

## Also per issue #9

- **PR #1** (`docs/claude-md-and-homebrew-cleanup`, open since 2026-05-09) is
  superseded: `CLAUDE.md` is already on `main`, and the formula it wanted to
  delete is already gone. Recommending closing it in a separate PR comment;
  not closing it or deleting its branch myself.
- No Homebrew tap, formula, or `tomstagl/homebrew-clawctl` repo created —
  that decision stands and reversing it is the owner's call.
- No edits to `tasks/prd-*.md`.
- No edits to `README.md`'s install section (already correct).
- No tag pushed, no release created.
- Not merging this PR myself.

## Test plan

- [x] `grep -rni "homebrew\|brew install\|brew tap" --include="*.md" .` — hits
      only in `tasks/prd-*.md`.
- [x] Re-read `docs/mcp.md` prerequisites and `CONTRIBUTING.md` Releasing
      section for accuracy against `release.yml` and `cmd/clawctl/main.go`.
- [ ] CI (no code changes; docs only).

---
_Generated by [Claude Code](https://claude.ai/code)_
